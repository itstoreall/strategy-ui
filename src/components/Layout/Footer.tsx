import Container, { Label } from '@/src/components/Container/Page';

const Footer = () => {
  return (
    <footer className="footer">
      <Container label={Label.Footer}>{''}</Container>
    </footer>
  );
};

export default Footer;
